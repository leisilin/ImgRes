在第幾個事件中從主身上獲得利益，沉積之後爲了獲得名利，以身博眼球，最終卻被新的娛樂記者做為成名的素材
福祿壽
七宗罪
鬼節，主一人在病院和不可見的人互動，其他人均沒出場，一輛裝氣體催眠劑的車，掉下一罐藥，整個醫院陷入半夢半醒的狀態。主要和鬼魂做游戲，否則所有人都要昏死

https://www.cnblogs.com/ysocean/p/8419559.html
Object源码
使用 java.lang 包下的所有类，都不需要手动导入。Java中的两种导包形式
①、单类型导入（single-type-import），例如import java.util.Date
②、按需类型导入(type-import-on-demand)，例如import java.util.*
  根据名字按需导入要知道他是按照需求导入，并不是导入整个包下的所有类。
  顶层路径名 \ 包名 \ 文件名.class = 绝对路径
按需类型导入则比较复杂，编译器会把包名和文件名进行排列组合，然后对所有的可能性进行类文件查找定位
不改变equeals方法，比较对象的equals始终是调用 Object 类的equals 方法，也就是用 == 比较两个对象在栈内存中的引用地址是否相等。
native 关键字 修饰一个由其他语言来实现的接口，为了与其他语言类库关联，由虚拟机实现。
 
 getClass和类名.class 获取这个类的类对象：class 是一个类的属性，能获取该类编译时的类对象，而 getClass() 是一个类的方法，它是获取该类运行时的类对象。
 对于 Map 集合，我们可以选取Java中的基本类型，还有引用类型 String 作为 key，因为它们都按照规范重写了 equals 方法和 hashCode 方法。但是如果你用自定义对象作为 key，那么一定要覆写 equals 方法和 hashCode 方法，不然会有意想不到的错误产生。
integer

String源码
一旦一个String对象被创建, 包含在这个对象中的字符序列是不可改变的, 包括该类后续的所有方法都是不能修改该对象的，直至该对象被销毁，
这是我们需要特别注意的（该类的一些方法看似改变了字符串，其实内部都是创建一个新的字符串，下面讲解方法时会介绍

一个 String 字符串实际上是一个 char 数组。：private final char value[];

String本质是一个char所以它的length（）方法实际上是char的length属性，isEmpty()判断的是判断的长度而不判断null

String hashCode 方法择数字31作为乘子，不大不小优选质数，且能被jvm优化，因为位运算31 * i = (i << 5) - i更加高效

jvm内存分布
程序计数器：也称为 PC 寄存器，保存的是程序当前执行的指令的地址（也可以说保存下一条指令的所在存储单元的地址），当CPU需要执行指令时，需要从程序计数器中得到当前需要执行的指令所在存储单元的地址，然后根据得到的地址获取到指令，在得到指令之后，程序计数器便自动加1或者根据转移指针得到下一条指令的地址，如此循环，直至执行完所有的指令。线程私有。

　　②、虚拟机栈：基本数据类型、对象的引用都存放在这。线程私有。

　　③、本地方法栈：虚拟机栈是为执行Java方法服务的，而本地方法栈则是为执行本地方法（Native Method）服务的。在JVM规范中，并没有对本地方法栈的具体实现方法以及数据结构作强制规定，虚拟机可以自由实现它。在HotSopt虚拟机中直接就把本地方法栈和虚拟机栈合二为一。

　　④、方法区：存储了每个类的信息（包括类的名称、方法信息、字段信息）、静态变量、常量以及编译器编译后的代码等。注意：在Class文件中除了类的字段、方法、接口等描述信息外，还有一项信息是常量池，用来存储编译期间生成的字面量和符号引用。

　　⑤、堆：用来存储对象本身的以及数组（当然，数组引用是存放在Java栈中的）
  
  Arrays工具类
  工具类下的asList方法返回的是一个定长arrayList是Arrays工具类的内部类，没有add和delete方法，其父类有方法，但是会报错UnsupportedOperationException
  
  把基本数据类型的数组作为参数执行asList方法，会发现生成的List长度是1，因为asList接收的是一个泛型集合，而基本数据类型没有泛型，只有集合有泛型，所以
  整个集合作为一个参数执行方法而不是集合里的每个数字，但如果用的包装类可以：如：int[]==>Integer[]
  
  asList返回的List中存储的是引用而不是新的对象，当修改返回的List中的值时，原本的集合中的值也会变化。
  建议asList只读不增删的情况下调用

类比禁闭岛，所有人都是在和主演戏，完了治疗主

反转，主被治愈，但事实上其他人并未演戏，一切都是真的，是为了压制能力，等到需要使用的时候再激活他
